# Configure in a trusted local process environment; this file is not auto-loaded.
OPENROUTER_API_KEY=
# Optional secondary credential; leave empty to keep single-key behavior.
OPENROUTER_API_KEY_FALLBACK=
OPENROUTER_MODEL=
# Optional trusted HTTPS API base only; it will receive your credential.
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
